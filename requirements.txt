# ============================================================
# Dependances Python — T'ELEFAN MES 4.0
# Versions testees sur Python 3.10 / Windows 10
# ============================================================

# --- Framework web ---
Flask==3.0.3
Flask-SQLAlchemy==3.1.1

# --- Base de donnees ---
PyMySQL==1.1.1                 # Connecteur Python pur pour MariaDB/MySQL

# --- Configuration ---
python-dotenv==1.0.1           # Chargement des variables d'environnement (.env)

# --- Traitement de donnees ---
pandas==2.2.2                  # Manipulation de DataFrames (calcul KPIs)
plotly==5.22.0                 # Graphiques interactifs (utilise cote client via CDN)

# --- Export ---
openpyxl==3.1.3                # Generation de fichiers Excel (.xlsx)
weasyprint==62.3               # Conversion HTML -> PDF
                               # /!\ Windows : necessite GTK3 (voir docs/INSTALLATION_VENV.md)
                               #     Si non installe, l'export bascule automatiquement en HTML

# --- Serveur standalone ---
waitress==3.0.0                # Serveur WSGI production (utilise par l'executable)

# --- Tests ---
pytest==8.2.2                  # Framework de tests unitaires
pytest-flask==1.3.0            # Fixtures Flask pour pytest (client HTTP, app context)

# ============================================================
# Variables d'environnement — T'ELEFAN MES 4.0
# Copier ce fichier en .env et adapter les valeurs
# ============================================================

# ----------------------------------------------------------------
# URL de connexion SQLAlchemy
# Format : mysql+pymysql://utilisateur:mot_de_passe@hote:port/nom_base
# ----------------------------------------------------------------

# >> Avec venv local (XAMPP ou MariaDB installe sur la machine) <<
DATABASE_URL=mysql+pymysql://example_user:example_password@localhost:3306/MES4

# >> Avec Docker Compose (docker-compose up) — decommenter cette ligne <<
# DATABASE_URL=mysql+pymysql://example_user:example_password@db:3306/MES4

# ----------------------------------------------------------------
# Cle secrete Flask
# Utilisee pour signer les cookies de session.
# En local/test : n'importe quelle chaine suffit.
# En production : generer une valeur aleatoire avec :
#   python -c "import secrets; print(secrets.token_hex(32))"
# ----------------------------------------------------------------
SECRET_KEY=exemple-secret-key-2025

# ----------------------------------------------------------------
# Configuration Flask
# FLASK_APP : chemin vers l'objet app (ne pas modifier)
# FLASK_DEBUG : 1 = mode debug (rechargement auto, erreurs detaillees)
#               0 = mode production
# ----------------------------------------------------------------
FLASK_APP=app.run:app
FLASK_DEBUG=1
